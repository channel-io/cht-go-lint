# consumer is deep enough to wall its own internals (recursion).
children:
  decode: { shared: true }                 # consumer-internal common
  handler: { may_import: [decode] }
  # pool is undeclared -> still a node (consumer is walling), a deny-default
  # sibling of decode/handler. It imports only decode (shared), so it stays clean.
