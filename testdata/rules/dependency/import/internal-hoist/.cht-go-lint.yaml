# internal/ is transparent; codec hoists to a kafka child node, granted only to consumer.
module: example.com/hoist
location: { strategy: node-tree }
roots: [pkg]
rules: { dependency/import: error }
children:
  kafka:
    children:
      producer: {}
      consumer: { may_import: [codec] } # -> hoisted internal/codec
