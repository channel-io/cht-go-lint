# Deliberately-broken project: every import marked WANT-VIOLATION must be reported.
module: example.com/bad
location:
  strategy: node-tree
roots: [pkg]
rules:
  dependency/import: error
children:
  sqlrepo: {}
  kafka:
    children:
      core: { shared: true }
      producer: {}
      consumer:
        may_import: [producer]
